apiVersion: v1
kind: Service
metadata:
  name: mysqldb
spec:
  type: "{{ .Values.service.type }}"
  selector:
    app: mysqldb
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
